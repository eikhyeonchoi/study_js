/**
 * ㅡㅡ핵심키워드
 * Math
 * 
 * ㅡㅡ개요
 */

// Math는 생성자함수가 아니라서 프로퍼티와 정적메서드만 제공함
{ // 프로퍼티
    // Math.PI: 파이값
}

{ // 메서드
    // Math.abs(값): 절대값
    // Math.round(값): 소수점 이하 반올림
    // Math.ceil(값): 소수점 이하 올림
    // Math.floor(값): 소수점 이하 내림
    // Math.sqrt(값): 제곱근 리턴
    // Math.random(): 0에서 1사이의 난수 
    // Math.pow(param1, param2): param1을 param2만큼 거듭제곱함
    // Math.max(인수들): 전달받은 인수 중 가장 큰 수 반환(배열, 스프레드문법 가능)
    // Math.min(인수들): 전달받은 인수 중 가장 작은 수 반환(배열, 스프레드문법 가능)
}